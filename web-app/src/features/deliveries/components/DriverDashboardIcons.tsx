import { Package, History, BarChart, AlertCircle, PackageSearch  } from "lucide-react";

export const DriverDashboardIcons = {
    package: Package,
    history: History,
    chart: BarChart,
    alert: AlertCircle,
    "package-search": PackageSearch
};