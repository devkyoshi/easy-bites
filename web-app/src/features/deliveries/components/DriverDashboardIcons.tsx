import { Package, History, BarChart, AlertCircle } from "lucide-react";

export const DriverDashboardIcons = {
    package: Package,
    history: History,
    chart: BarChart,
    alert: AlertCircle,
};