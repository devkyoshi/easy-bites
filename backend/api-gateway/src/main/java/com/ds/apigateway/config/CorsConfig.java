package com.ds.apigateway.config;

import org.springframework.context.annotation.Configuration;

/**
 * CORS configuration is now handled in SecurityConfig.java to ensure proper integration with Spring Security.
 * This class is kept for reference but is no longer active.
 */
@Configuration
public class CorsConfig {
    // CORS configuration moved to SecurityConfig.java
}
